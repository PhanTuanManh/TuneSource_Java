// Controller: ArtistController class extending UserController for artist-specific functionalities
public class ArtistController extends UserController {
    public ArtistController(Artist model, UserView view) {
        super(model, view);
    }

    // Additional methods for handling artist-specific interactions
}
